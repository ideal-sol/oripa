{
  "name": "@site/invalid-storefront",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test:contract": "node scripts/contract-test.mjs"
  },
  "dependencies": {
    "@oripa/site-schema": "workspace:*",
    "@oripa/storefront-client": "^2.0.0"
  }
}
